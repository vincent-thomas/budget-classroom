<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body class="h-screen grid grid-rows-[auto_1fr_auto]">
    <header class="flex justify-between w-full py-3 px-4 border-b border-1">
      <h1 class="text-lg font-semibold italic">Budget classroom</h1>
      <nav class="flex gap-4 items-center">
        <a rel="preload" href="/profile">
          <img
            src="https://lh3.googleusercontent.com/ogw/AF2bZygWcZbwkt65qQ9ed_srE5Xfft8FTMoUquLKSaBCwWNLTw=s32-c-mo"
            width="40"
            height="40"
            class="rounded-full"
          >
        </a>
      </nav>
    </header>
    <main class="p-3 overflow-scroll">
      <div class="max-w-[800px] mx-auto px-3 flex flex-col gap-4">
        <div class="bg-blue-500 p-4 rounded-lg">
          <h2 class="text-white text-2xl font-semibold"><%= @room["name"] %></h2>
        </div>

        <div class="space-y-4">
          <% @room_items.each do |item| %>
            <a href="/classes/<%= @room["id"] %>/items/<%= item["id"] %>" class="block">
              <div class="bg-white border rounded-lg p-4 hover:shadow-md transition-shadow">
                <div class="flex justify-between items-start">
                  <div class="space-y-2">
                    <h3 class="text-xl font-semibold"><%= item["title"] %></h3>
                    <p class="text-gray-600"><%= item["description"] %></p>
                    <% if item["material_name"] %>
                      <div class="flex items-center gap-2 text-blue-600">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                        </svg>
                        <span class="text-sm"><%= item["material_name"] %></span>
                      </div>
                    <% end %>
                  </div>
                  <span class="text-sm text-gray-500">
                    <%= item["created_at"].split(' ')[0] %>
                  </span>
                </div>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    </main>
    <%= render_component :footer %>
  </body>
</html>

